name: CD (Beta 環境)

on:
  push:
    branches: [beta]
  workflow_dispatch:

jobs:
  call-deploy:
    uses: ./.github/workflows/deploy-template.yaml

    with:
      deploy_host: ${{ secrets.TEST_EC2_HOST }}
      branch: beta
      tag: beta
