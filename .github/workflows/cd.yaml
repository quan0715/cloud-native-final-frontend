name: CD

on:
  push:
    branches: [main]

jobs:
  call-deploy:
    uses: ./.github/workflows/deploy-template.yaml
    with:
      deploy_host: ${{ secrets.EC2_HOST }}
      branch: main
      tag: latest
